import React from 'react';
import './NotFound.css';

const NotFound = (props) => {


    return (
        <div className="NotFound">
            NotFound
        </div>
    );
}

export default NotFound;